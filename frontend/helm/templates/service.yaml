apiVersion: v1
kind: Service
metadata:
  name: {{ include "servicepro-frontend.fullname" . }}
  labels:
    {{- include "servicepro-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "servicepro-frontend.selectorLabels" . | nindent 4 }}
